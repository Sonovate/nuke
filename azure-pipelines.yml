# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [TestAzurePipelines (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration AzurePipelines --host AzurePipelines
#
# </auto-generated>
# ------------------------------------------------------------------------------

parameters:
  - name: Verbosity
    type: object

variables:
  - group: variable-group-1
  - name: GitVersion
    value:
  - name: StringVariable
    value: var
  - name: Verbosity
    value: ${{ parameters.Verbosity }}

trigger:
  batch: true
  branches:
    include:
      - included_branch
    exclude:
      - excluded_branch
  tags:
    include:
      - included_tags
    exclude:
      - excluded_tags
  paths:
    include:
      - included_path
    exclude:
      - excluded_path

stages:
  - stage: ubuntu_18_04
    displayName: 'ubuntu-18.04'
    dependsOn: [  ]
    pool:
      vmImage: 'ubuntu-18.04'
    jobs:
      - job: A
        displayName: 'A'
        dependsOn: [  ]
        steps:
          - task: CmdLine@2
            inputs:
              script: './build.cmd A --skip'
            env:
              AzurePipelinesSystemAccessToken: $(System.AccessToken)
              GitHubToken: $(GitHubToken)
      - job: B
        displayName: 'B'
        dependsOn: [ A ]
        variables:
          GitVersion: $[ dependencies.A.outputs['CmdLine.GitVersion'] ]
          StringVariable: $[ dependencies.A.outputs['CmdLine.StringVariable'] ]
        steps:
          - task: CmdLine@2
            inputs:
              script: './build.cmd B --skip'
            env:
              AzurePipelinesSystemAccessToken: $(System.AccessToken)
              GitHubToken: $(GitHubToken)